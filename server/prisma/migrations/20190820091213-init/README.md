# Migration `20190820091213-init`

This migration has been generated by Vimalraj Selvam at 8/20/2019, 9:12:13 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."TodoItem"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,"completed" BOOLEAN NOT NULL DEFAULT false ,"createdAt" DATE NOT NULL  ,"updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,PRIMARY KEY ("id"))
;

CREATE UNIQUE INDEX "lift"."TodoItem.id._UNIQUE" ON "TodoItem"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20190820091213-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,17 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model TodoItem {
+  id        String   @default(cuid()) @id @unique
+  name      String
+  completed Boolean  @default(false)
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20190820091213-init)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190820091213-init'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
